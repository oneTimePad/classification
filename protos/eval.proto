syntax = "proto2";

package classification.protos;


message EvalConfig {
    //how many samples to run through evaluation
    required uint32 num_examples = 1;
    //where to restore from
    required string checkpoint_to_restore = 2;
}
